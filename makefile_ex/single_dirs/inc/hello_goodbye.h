// Prevent duplication of recipies
#pragma once

// Angle brackets are from installed libraries
#include <iostream>

// Also include hello.h because we are going to use its function
#include "hello.h"

// Define the name of a function, its args, and what it returns
void hello_goodbye();
